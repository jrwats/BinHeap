{
  "name": "bin-heap-js",
  "version": "0.0.3",
  "description": "Simple Binary Heap implementation for JavaScript",
  "main": "index.js",
  "repository": "git@github.com:jrwats/BinHeap.git",
  "author": "John Watson <jrwats@gmail.com>",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "jest": "^25.3.0"
  },
  "scripts": {
    "test": "yarn jest",
    "min-build": "yarn babel src/BinHeap.js --no-comments --minified -o lib/BinHeap.min.js",
    "build": "mkdir -p lib && cp src/BinHeap.js lib && yarn min-build",
    "clean": "rm -rf lib"
  }
}
